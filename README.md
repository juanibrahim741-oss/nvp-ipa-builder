# NVP Node — unsigned iOS IPA builder

Builds an UNSIGNED .ipa (no code signing, **no bundled models** — uses CoreML shards / on-demand).
Run: Actions → "iOS Unsigned IPA" → Run workflow, or push to `ios/**`.
